#include<bits/stdc++.h>
using namespace std;
int main(){
    int m,n; cin>>m>>n;
    if(m>n) m=n;
        cout<<((min(n,m)%2!=0) ? "Akshat":"Malvika");}//cout<<((m%2==0) ? "Malvika":"Akshat");}

